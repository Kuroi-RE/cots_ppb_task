import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF6366F1);
  static const Color primaryDark = Color(0xFF4F46E5);
  static const Color primaryLight = Color(0xFF818CF8);

  // Background colors
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF1F5F9);

  // Text colors
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textTertiary = Color(0xFF94A3B8);

  // Status colors
  static const Color statusBerjalan = Color(0xFF3B82F6); // Blue
  static const Color statusSelesai = Color(0xFF10B981); // Green
  static const Color statusTerlambat = Color(0xFFEF4444); // Red

  // Status background colors (lighter versions)
  static const Color statusBerjalaanBg = Color(0xFFDCEFFE);
  static const Color statusSelesaiBg = Color(0xFFD1FAE5);
  static const Color statusTerlambatBg = Color(0xFFFEE2E2);

  // Border colors
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderFocus = Color(0xFF6366F1);

  // Error colors
  static const Color error = Color(0xFFDC2626);
  static const Color errorBg = Color(0xFFFEF2F2);

  // Success colors
  static const Color success = Color(0xFF059669);

  // Disabled colors
  static const Color disabled = Color(0xFFCBD5E1);

  // Shadow colors
  static const Color shadow = Color(0x1A000000);
}
